{
  "name": "@types/cors",
  "version": "2.7.0",
  "description": "Types for https://github.com/expressjs/cors",
  "scripts": {
    "test": "npm run bundle && npm run spec",
    "spec": "ts-node test/index.ts",
    "bundle": "typings bundle . --out bundle.d.ts",
    "prepublish": "typings install"
  },
  "devDependencies": {
    "cors": "^2.7.1",
    "ts-node": "^1.3.0",
    "tslint": "^3.7.4",
    "tslint-config-typings": "^0.2.3",
    "typescript": "^1.8.9",
    "typings": "^1.3.3"
  }
}
